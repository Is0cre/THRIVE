name: thrives_app
description: "THRIVES — Privacy-first trauma regulation platform."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.6.2

dependencies:
  flutter:
    sdk: flutter
  just_audio: ^0.9.42
  flutter_animate: ^4.5.2
  shared_preferences: ^2.3.3
  vibration: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/audio/
